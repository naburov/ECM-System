<UserControl x:Class="СЭД_ЭК.btnCurveSmall"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:СЭД_ЭК"
             mc:Ignorable="d" Height="86.996" Width="226.73">
    <Grid  Width="220" Height="80" Cursor="Hand">
        <Button
                    BorderThickness = "0">
            <Button.Template>
                <ControlTemplate>
                    <Border CornerRadius="30"
                                BorderBrush="#0B9AEC"
                                BorderThickness="0"
                                Background="#0B9AEC"/>
                </ControlTemplate>
            </Button.Template>
        </Button>
        <Label Name="lbl" Content="Войти" Foreground="White"
               HorizontalAlignment="Center" VerticalAlignment="Center"
               Loaded="lbl_Loaded"
               FontFamily="HelveticaNeueCyr"
               FontWeight="Light"
               FontSize="20" Margin="14.527,21.275,10,18.075" Width="195.473"/>
    </Grid>
</UserControl>
